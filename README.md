# Slack Markdown Exporter

## 🌟 Обзор проекта

**Slack Markdown Exporter** — это расширение для Google Chrome, предназначенное для быстрой и удобной выгрузки истории сообщений из любого активного диалога Slack (канала или личного чата) в формате Markdown (`.md`).

Расширение позволяет пользователю выбрать, **сколько последних сообщений** необходимо экспортировать, и сохраняет ключевые элементы форматирования (жирный текст, курсив, ссылки, блоки кода, цитаты), а также включает **полные временные метки** и имя автора для каждого сообщения.

---

## 🛠️ Функциональные возможности

* **Выбор количества сообщений:** Возможность указать, сколько последних сообщений (N) экспортировать, с помощью поля ввода.
* **Сохранение форматирования:** Конвертация внутреннего HTML-форматирования Slack в синтаксис Markdown:
    * Жирный текст (`**текст**`).
    * Курсив (`*текст*`).
    * Код в строке (`` `код` ``).
    * Блоки кода (```` ```code block````).
    * Цитаты (`> цитата`).
    * Ссылки (`[текст](url)`).
* **Полные временные метки:** Каждое сообщение включает дату и время отправки с точностью до секунд.
* **Автоматическая загрузка:** Экспортированный `.md` файл автоматически скачивается на компьютер.

---

## 🚀 Установка и использование

Поскольку это пользовательское расширение, его необходимо установить в **Режиме разработчика** (`Developer mode`) Chrome.

### Шаг 1: Подготовка файлов

1.  Создайте на компьютере новую папку, например, `slack-md-exporter`.
2.  Поместите в эту папку следующие файлы (с кодом, предоставленным "Партнером программиста"):
    * `manifest.json`
    * `content.js`
    * `style.css`
    * `icon.png` (Любая иконка, например, 128x128px)

### Шаг 2: Установка в Chrome

1.  Откройте Google Chrome и перейдите по адресу: `chrome://extensions`.
2.  Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу страницы.
3.  Нажмите кнопку **"Загрузить распакованное расширение"** (`Load unpacked`).
4.  Выберите папку `slack-md-exporter`.

Расширение будет успешно установлено и активно.

### Шаг 3: Экспорт

1.  Откройте веб-версию Slack (`https://app.slack.com/`) и перейдите в нужный канал или диалог.
2.  Рядом с вкладками диалога (Messages, Files и т.д.) появится **поле ввода** и кнопка **"Экспорт MD"**.
3.  Введите желаемое количество последних сообщений для экспорта (например, `20`).
4.  Нажмите **"Экспорт MD"**.
5.  Файл (`[Название_Канала]_export.md`) будет автоматически загружен.
